meta {
  name: Logout
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/auth/logout
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("status 204", function() {
    expect(res.status).to.equal(204);
  });
}
