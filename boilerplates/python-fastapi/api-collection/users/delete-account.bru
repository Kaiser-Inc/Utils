meta {
  name: Delete Account
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/users/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("status 204", function() {
    expect(res.status).to.equal(204);
  });
}
